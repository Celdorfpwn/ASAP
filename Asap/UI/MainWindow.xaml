<UserControl x:Class="SushiPikant.UI.MainWindow"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="500" d:DesignWidth="800" MinHeight="500" MinWidth="900"
             Name="Main" Background="White">
 
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="0"></RowDefinition>
                <RowDefinition Height="*"></RowDefinition>
            </Grid.RowDefinitions>


            <ContentControl Name="TopTools" Grid.Row="0">
                <Border Grid.Column="1"  BorderThickness="0,0,0,0" BorderBrush="#3366FF">
                    <TextBlock Text="{Binding Logs}" VerticalAlignment="Stretch" HorizontalAlignment="Stretch"></TextBlock>
                </Border>
            </ContentControl>
            <ContentControl Name="ContentContainer" Grid.Row="1">
            </ContentControl>
        </Grid>
    
</UserControl>
